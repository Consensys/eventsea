{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "deploy:test": "turbo run deploy:test",
    "deploy:local": "turbo run deploy:local",
    "compile": "turbo run compile",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.4",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "*",
    "turbo": "latest"
  },
  "name": "@consensys/eventsea",
  "packageManager": "npm@9.6.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
